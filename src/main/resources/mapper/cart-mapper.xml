<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Cart">

    <insert id="addCart" parameterType="com.example.demo.dto.CartDTO">
        INSERT INTO cart (custid, bookid, count)
        VALUES (#{custid}, #{bookid}, #{count})
        ON DUPLICATE KEY UPDATE count = count + #{count}
    </insert>

    <select id="getCartList" parameterType="int" resultType="com.example.demo.dto.CartDTO">
        SELECT c.cart_id, c.custid, c.bookid, c.count,
               b.bookname, b.price, b.cover_image_path,
               (c.count * b.price) AS total_price
          FROM cart c
          JOIN book b ON c.bookid = b.bookid
         WHERE c.custid = #{custid}
    </select>

    <delete id="deleteCart" parameterType="int">
        DELETE FROM cart WHERE cart_id = #{cartId}
    </delete>
    
    <delete id="clearCart" parameterType="int">
        DELETE FROM cart WHERE custid = #{custid}
    </delete>
    
    <delete id="deleteAll">
        DELETE FROM cart
    </delete>
</mapper>